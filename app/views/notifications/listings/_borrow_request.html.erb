<% @actor = User.find(notification.actor_id) %>
<div class="d-flex flex-row">
    <%= image_tag 'empty-user-icon.png', class:"rounded-circle ml-2 mt-1", style:'width:50px;height:50px' %>
    <%= link_to " #{@actor.name} has requested to borrow your item: #{notification.notifiable.title}", 
    user_path(current_user), class: "dropdown-item"%>
</div>