<% @actor = User.find(notification.actor_id) %>
<%= link_to " #{@actor.name} has joined a group you follow: #{notification.notifiable.name}", group_path(notification.notifiable), class:'dropdown-item' %>