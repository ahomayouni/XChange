<%= provide(:title, 'All Users') %>
<h1> All Users </h1>

<%= will_paginate %>

<ul>
	<% @users.each do |user| %>
		<li>
			<%= user.name %>
			<%= link_to "View Profile", user_path(user) %>
		</li>
	<%end%>
</ul>

<%= will_paginate %>