<% provide(:title, "Log in") %>

<div class="signin-container">
  <div class="signin-form p-4 m-auto">
    <h2 class="text-center">Log in</h2>
    <div>
      <%= render 'form' %>
    </div>
  </div>
  <p class="text-center"> New user? <%= link_to "Sign up now!", new_user_path %></p>
  <%# Don't think we need this since we have home link on footer now %>
  <%# <%=link_to "Back to Home", home_index_path %>
</div>