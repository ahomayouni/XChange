<% if logged_in? %>
	<!-- current_user is actually a function call living in sessions_helper.rb. Returns a @user object -->
	 <nav class="navbar navbar-expand-lg navbar-light bg-light">
		<a class="navbar-brand" href="#">XChange</a>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
		<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
			<div class="navbar-nav">
				<li class="nav-item nav-link active">
					<%= link_to "#{current_user.name.camelcase}", user_path(current_user) %>
				</li>
				<li class="nav-tem nav-link active">
					<%= link_to "Users", users_path %>
				</li>
				<li class="nav-tem nav-link active">
					<%= link_to "Listings", listings_path %>
				</li>
				<li class="nav-tem nav-link active">
					<%= link_to "Settings", edit_user_path(current_user) %> 
				</li>
				<li class="nav-tem nav-link active">
					<%= link_to "Log Out", logout_path , method: :delete %>
				</li>
			</div>
		</div>
   </nav>
<%end%>

<% if !logged_in? %>
	<nav class="navbar navbar-expand-lg navbar-light bg-light">
		<a class="navbar-brand" href="#">XChange</a>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
		<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
			<div class="navbar-nav">
				<li class="nav-item nav-link active">
					<%= link_to "Log In", login_path %>
				</li>
			</div>
		</div>
	</nav>
<%end%>