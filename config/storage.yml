test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Use rails credentials:edit to set the AWS secrets (as aws:access_key_id|secret_access_key)
#amazon:
#  service: S3
#  access_key_id: AKIAIUPINSKRRVFCXIMQ
#  secret_access_key: Iaq3d1Ptn7mHuTXzjF4U1FSYUHLI0J5DKy9aaXXb
#  region: us-east-1
#  bucket: active-storage-xchange

# Remember not to checkin your GCS keyfile to a repository
google:
  service: GCS
  project: xchange
  credentials: 
  {
    provider: 'Google',
    google_storage_access_key_id: 'GOOGOB4BLCGQCXA6W5AOKIEL',
    google_storage_secret_access_key: 'ONKV09fpjebsVRIGgSNm6P/lcS958cPJBs4nrwwy'
  }
  bucket: xchangecsc444

# Use rails credentials:edit to set the Azure Storage secret (as azure_storage:storage_access_key)
# microsoft:
#   service: AzureStorage
#   storage_account_name: your_account_name
#   storage_access_key: <%= Rails.application.credentials.dig(:azure_storage, :storage_access_key) %>
#   container: your_container_name

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
